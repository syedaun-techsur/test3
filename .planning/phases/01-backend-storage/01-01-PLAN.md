---
phase: 01-backend-storage
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - .gitignore
  - .env.example
  - data/.gitkeep
  - public/index.html
  - public/style.css
  - public/app.js
autonomous: true

features:
  implements: ["PERS-01", "F4"]
  depends_on: []
  enables: ["F0", "F1", "F2", "F3"]

must_haves:
  truths:
    - "npm install completes without errors"
    - "All required dependencies are listed in package.json (express, better-sqlite3, helmet, cors, dotenv)"
    - "Development dependencies are present (nodemon, jest, supertest)"
    - "node_modules/, data/, and .env are excluded from git"
    - "Project directory structure matches TechArch spec exactly"
  artifacts:
    - path: "package.json"
      provides: "Dependency manifest and npm scripts"
      contains: "better-sqlite3"
    - path: ".gitignore"
      provides: "Git exclusions for data/, .env, node_modules/"
    - path: ".env.example"
      provides: "Environment variable documentation"
      contains: "DATABASE_URL"
    - path: "public/index.html"
      provides: "Static frontend shell placeholder (Phase 2 content)"
    - path: "public/style.css"
      provides: "Empty CSS placeholder"
    - path: "public/app.js"
      provides: "Empty JS placeholder"
  key_links:
    - from: "package.json"
      to: "node_modules/"
      via: "npm install"
      pattern: "better-sqlite3.*9"
    - from: ".env.example"
      to: ".env"
      via: "developer copies file"
      pattern: "DATABASE_URL"
---

<objective>
Scaffold the project: create package.json with all required dependencies, set up the directory structure from TechArch §6.4, create .gitignore, and placeholder static files so all subsequent plans have a working Node.js project to build on.

Purpose: Every other plan depends on this foundation — db.js needs better-sqlite3, server.js needs express and helmet, tests need jest and supertest. Without a correct package.json and npm install, nothing else can execute.
Output: A fully initialized Node.js project with dependencies installed, directory structure in place, and git-safe configuration.
</objective>

<feature_dependencies>
Implements: PERS-01: Tasks persist between page refreshes via server-side storage (infrastructure foundation); F4: Task Persistence (project scaffold enables durable storage implementation)
Depends on: None
Enables: F0: Task Creation, F1: Task List View, F2: Task Completion, F3: Task Deletion (all require the running server built on this scaffold)
</feature_dependencies>

<execution_context>
@/app/workspaces/.pivota-home/opencode-xdg/opencode/pivota_spec-framework/workflows/execute-plan.md
@/app/workspaces/.pivota-home/opencode-xdg/opencode/pivota_spec-framework/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-backend-storage/01-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create package.json with all dependencies and npm scripts</name>
  <files>package.json</files>
  <action>
Create `package.json` at the project root with the following content. Use exact versions from TechArch §6.1 and §6.3:

```json
{
  "name": "todoapp",
  "version": "1.0.0",
  "description": "Basic To-Do App — REST API with SQLite persistence",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "nodemon server.js",
    "test": "jest --forceExit"
  },
  "dependencies": {
    "better-sqlite3": "^9.0.0",
    "cors": "^2.8.5",
    "dotenv": "^16.0.0",
    "express": "^4.18.0",
    "helmet": "^7.0.0"
  },
  "devDependencies": {
    "jest": "^29.0.0",
    "nodemon": "^3.0.0",
    "supertest": "^6.0.0"
  },
  "jest": {
    "testEnvironment": "node"
  }
}
```

Note: `cors` is added per the user's locked decision (CORS: allow all origins, `Access-Control-Allow-Origin: *`). `dotenv` is added for PORT and DATABASE_URL env var loading. After writing the file, run `npm install` to install all dependencies.
  </action>
  <verify>
```bash
npm install 2>&1 | tail -5
node -e "require('express'); require('better-sqlite3'); require('helmet'); require('cors'); require('dotenv'); console.log('ALL DEPS OK')"
```
  </verify>
  <done>
`npm install` exits 0 with no errors. `node -e "require(...)"` prints "ALL DEPS OK" for all five production dependencies. `node_modules/` exists. `package-lock.json` is created.
  </done>
</task>

<task type="auto">
  <name>Task 2: Create .gitignore, .env.example, directory structure, and public placeholders</name>
  <files>
    .gitignore
    .env.example
    data/.gitkeep
    public/index.html
    public/style.css
    public/app.js
  </files>
  <action>
Create the following files exactly as specified:

**`.gitignore`** — excludes runtime artifacts from version control:
```
node_modules/
data/
.env
*.log
```

**`.env.example`** — documents all environment variables (from TechArch §1.3):
```
# HTTP server port (default: 3000)
PORT=3000

# Path to SQLite database file (default: ./data/todo.db)
DATABASE_URL=./data/todo.db

# Runtime environment
NODE_ENV=development
```

**`data/.gitkeep`** — empty file to track the `data/` directory in git while keeping `data/todo.db` gitignored. The `.gitignore` excludes `data/` but `.gitkeep` ensures the directory exists for the DB file.

Wait — since `.gitignore` has `data/`, the `.gitkeep` won't be tracked. Instead: create the `data/` directory (it will be created by db.js at runtime anyway). Skip `.gitkeep`. Just ensure the directory exists by creating it.

Actually: create `data/` directory. The SQLite DB file will be created there by db.js at runtime. Since `data/` is gitignored, the directory must be created at runtime or by setup. Add a note in `.env.example` about this.

**`public/index.html`** — minimal HTML shell placeholder (Phase 2 will replace this):
```html
<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>TodoApp</title>
  <link rel="stylesheet" href="/style.css">
</head>
<body>
  <h1>TodoApp</h1>
  <p>Backend API is running. Frontend coming in Phase 2.</p>
  <script src="/app.js"></script>
</body>
</html>
```

**`public/style.css`** — empty placeholder:
```css
/* Phase 2: styles will be added here */
```

**`public/app.js`** — empty placeholder:
```javascript
// Phase 2: frontend logic will be added here
```

After creating all files, create the `data/` directory if it doesn't exist:
```bash
mkdir -p data
```
  </action>
  <verify>
```bash
ls -la .gitignore .env.example public/index.html public/style.css public/app.js
ls -la data/ 2>/dev/null || echo "data dir check"
node -e "const fs=require('fs'); ['package.json','.gitignore','.env.example','public/index.html','public/style.css','public/app.js'].forEach(f => { if(!fs.existsSync(f)) throw new Error('Missing: '+f); }); console.log('ALL FILES EXIST')"
```
  </verify>
  <done>
All six files exist. `.gitignore` contains `node_modules/`, `data/`, and `.env`. `.env.example` contains `PORT`, `DATABASE_URL`, and `NODE_ENV`. `public/index.html` is valid HTML with a `<title>TodoApp</title>`. `public/style.css` and `public/app.js` exist (can be empty/placeholder). `data/` directory exists.
  </done>
</task>

</tasks>

<verification>
```bash
# Verify all required files exist
node -e "
const fs = require('fs');
const required = [
  'package.json', '.gitignore', '.env.example',
  'public/index.html', 'public/style.css', 'public/app.js'
];
required.forEach(f => {
  if (!fs.existsSync(f)) throw new Error('MISSING: ' + f);
});
console.log('ALL REQUIRED FILES PRESENT');
"

# Verify all production deps installable
node -e "
require('express');
require('better-sqlite3');
require('helmet');
require('cors');
require('dotenv');
console.log('ALL DEPS LOADABLE');
"

# Verify scripts exist in package.json
node -e "
const pkg = require('./package.json');
['start','dev','test'].forEach(s => {
  if (!pkg.scripts[s]) throw new Error('MISSING SCRIPT: ' + s);
});
console.log('ALL SCRIPTS PRESENT');
"
```
</verification>

<success_criteria>
- `npm install` exits 0
- All five production deps (express, better-sqlite3, helmet, cors, dotenv) can be required without error
- All three dev deps (jest, nodemon, supertest) are in devDependencies
- `.gitignore` excludes node_modules/, data/, .env
- `.env.example` documents PORT, DATABASE_URL, NODE_ENV
- `public/` directory has index.html, style.css, app.js
- `data/` directory exists for SQLite file
</success_criteria>

<output>
After completion, create `.planning/phases/01-backend-storage/01-01-SUMMARY.md`
</output>
